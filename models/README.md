# Models Directory

This directory contains model architectures copied from the cloned repositories.

## Files

- `custom_resnet.py` - Pre-trained ResNet models from RVTALL-Preprocess
- `trainer.py` - Training utilities

## Usage

These models use PyTorch Hub to load ImageNet pre-trained weights, then fine-tune on RF spectrograms.

